"use strict"; //-> for new js versions nand treat old code as a new one it's by default but good practice
 //number 
 //bigint
 //string="" (use "", more readable)
 //boolean
 //null -> standalone value(empty value ) it's an object
 //undefined => value not assigned
 //symbol  => unique
 //object
